const config = {
	// API接口，必须是HTTPS开头
	"api": "https://localhost",
	// 版本号，禁止私自修改，后果自负
	"version": "1.0.0",
	// H5网页（用于分享）
	"h5": "https://localhost",
	// 首页分享处描述
	"description": "rule",

	// websocket
	"websocket": "wss://localhost/websocket"
}

// 导出配置
export default config;